{{- if .Values.services.proxy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "daytona.fullname" . }}-proxy
  namespace: {{ include "daytona.namespace" . }}
  labels:
    {{- include "daytona.labels" . | nindent 4 }}
    app.kubernetes.io/component: proxy
  {{- with .Values.services.proxy.autoscaling.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "daytona.fullname" . }}-proxy
  minReplicas: {{ .Values.services.proxy.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.services.proxy.autoscaling.maxReplicas | default 10 }}
  {{- if or .Values.services.proxy.autoscaling.targetCPUUtilizationPercentage .Values.services.proxy.autoscaling.targetMemoryUtilizationPercentage .Values.services.proxy.autoscaling.metrics }}
  metrics:
    {{- if .Values.services.proxy.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.proxy.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.services.proxy.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.proxy.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
    {{- if .Values.services.proxy.autoscaling.metrics }}
    {{- toYaml .Values.services.proxy.autoscaling.metrics | nindent 4 }}
    {{- end }}
  {{- end }}
  {{- if .Values.services.proxy.autoscaling.behavior }}
  behavior:
    {{- toYaml .Values.services.proxy.autoscaling.behavior | nindent 4 }}
  {{- end }}
{{- end }}
